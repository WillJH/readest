# Fork 开发路线图

本 fork 的功能规划与进度记录。上游同步策略:fork `main` 始终镜像 `upstream/main`,功能分支独立开发。

## ✅ 已完成

| 功能 | 分支/提交 | 说明 |
|------|-----------|------|
| 生词本 | `feat/vocab-builder` | 查词收藏(全词典释义快照 + 语境句子)、笔记本内浏览、跳回原文(带进度保护)、`.nomedia` 媒体扫描隔离 |
| AI 多角色助手 | `feat/ai-characters` | 角色卡(人设 + 头像图库)、AI 自选头像(`[avatar:]` 协议)、分层提示词(全局系统提示词 + 用户指令) |
| 多模型连接 | `feat/ai-characters` | 连接档案管理、每角色绑定连接(coding plan 端点友好)、嵌入模型保持全局 |
| 背景图定时轮换 | `feat/ai-characters` | 间隔轮换、随机/顺序、自选图片池 |
| Readest Dev 安卓包 | 本地 | `com.bilingify.readest.dev` 独立包名,与官方客户端共存 |

## 🔜 排队中

### 1. 📋 人物卡 + 关系图谱「这是谁来着?」(AI 功能,架构已定为增量知识库)

**核心场景**:读外文长篇小说(尤其俄文名著类),人物多、名字陌生、隔几天回来记不住谁是谁。

**架构决策(2026-08-29 定稿):增量式人物知识库,不依赖全书索引**

取代最初"每次调用按需 RAG"的思路。理由:

- **防剧透是构造保证**:只处理已读章节,未来内容从不进入知识库(索引方案是"全入库 + 检索时过滤"的软约束)
- **不依赖嵌入 API**:章节级处理只需聊天模型(coding plan 端点可直接用);全书索引需要嵌入端点
- **知识可浏览、可压缩**:人物卡 UI 直接读库(零调用零等待);对话时注入几 KB 的结构化人物表,而非几万 token 原文段落
- **进度自知**:库带 `processedThroughChapter` 指针,UI 显示"记忆至第 N 章 / 你读到第 M 章";系统提示词声明"你的记忆覆盖到第 N 章,之后出现的人物你不认识"——助手明确知道自己知道到哪

**数据模型(每书一份)**:

```
processedThroughChapter: N     ← 知识进度指针
characters: [{ 名字, 别名[], 滚动小结, 首次登场章节, 出现章节列表 }]
relationships: [{ A, B, 关系描述, 最后更新章节 }]
chapters: [{ 章节号, 一句话梗概 }]   ← 落地底稿,防滚动总结漂移;小结定期从底稿重生成
```

**流水线**:检测已读未处理的章节 → 每章一次 AI 调用(章文 + 现有知识快照 → 严格 JSON 增量)→ 更新库、推进指针。触发:手动"更新人物记忆"按钮(与"开始索引"并列,排在前面)+ 人物卡打开时提示落后章数。

**交互**:

- 入口:选中文本中的名字 → 工具条"人物卡";笔记本内人物面板;角落常驻头像的延伸
- 人物卡:人物小结 + 关系图(节点/边随进度生长)+ 登场章节列表(可跳原文)+ 知识进度条
- AI 对话:系统提示词注入压缩人物表 + 进度声明(与划词问 AI 组合:知识库管人物情节,划词管原文,索引降级为可选)

**已知打磨点**:

- 别名/昵称合并(外文小说的硬骨头),AI 辅助识别 + 人工合并入口
- 滚动总结漂移 → 每章底稿锚定,结论标注章节号
- 无清晰章节的 TXT → 按 section 粒度兜底

**依赖基建**:AI 连接系统、防剧透系统提示词、跳转基建、companionGenerator 式直连生成管道(已验证)、图谱选型(d3-force / cytoscape / 自绘 SVG)。

**预估**:中等偏大(人物抽取质量为主要打磨点)。

### 2. 📋 MCP 支持(Model Context Protocol)

让 AI 助手能连接 MCP 服务器,把外部工具(联网搜索、网页读取、知识库查询等)交给模型调用。要点:

- 传输:web 端仅支持 HTTP/SSE 传输(stdio 需要桌面端 Tauri 侧配合,二期)
- 配置:MCP 服务器列表(名称 + URL + 可选鉴权头),存 settings,与 AI 连接体系并列
- 管道:用官方 MCP TypeScript SDK 建立客户端会话,把 tools 桥接进 `streamText` 的 tools 参数(生成管道已有先例:Reedy 的 lookupPassage 工具)
- 安全:工具调用结果进入上下文前过一道提示词纪律(book passages 同款"数据非指令"约束)
- 风险:MCP 服务器质量参差;失败要优雅降级为"工具不可用"而非中断对话

## 🧊 已搁置

### 陪伴模式(Companion Mode)——设计待成熟

完整原型曾实现于 `feat/companion-mode` 分支(触发器状态机 + 直连生成 + 气泡通知,全量测试绿),因整体设计尚不成熟主动撤下,主线不再包含。重启时可直接从该分支取材,经验教训:

- **架构结论(已验证)**:绕开 assistant-ui runtime、直连 provider 生成 + 写入自有会话存储 + 气泡通知的链路完全可行——无需运行时注入,该风险点已消解
- **待想清楚的问题**:触发时机与分寸感的规则化表达、"陪伴"与"打扰"的边界、消息价值的评估方式、省 token 策略;这些想清楚之前不做
- 代码资产:companionTrigger(纯函数+单测)、companionGenerator(直连生成,可复用于人物卡等按需生成场景)、CompanionBubble

## 💡 候选池(brainstorm 备档,未排期)

- 个人化前情提要:重开书时基于"你的"高亮/生词/停留位置生成 recap(非全书摘要)
- 广播剧朗读:对话说话人识别 + 多声线 TTS
- 渐进式辅助阅读:句子级难度挂翻译,随水平自动变稀("拆辅助轮")
- 生词本 → EPUB 词书:用 send/conversion 管线把生词+语境排版成真书
- 困惑热图:页面停留时长异常 → 被动收集难句 → 批量 AI 讲解
- 局域网共读:LocalSend 基建上的双端翻页同步

## 基建备忘

- 容器内安卓构建链已就位:JDK17 + rust(aarch64)+ SDK/NDK(`/opt/android-sdk`)+ 签名密钥(`/root/keys/readest-dev.keystore`),`gen/android` 的 applicationId 补丁为本地未提交状态
- web 开发命令:`API_BASE_URL=https://web.readest.com NEXT_DEV_ALLOWED_ORIGINS='...' pnpm dev-web -p 41790`
- 构建产物:`gen/android/app/build/outputs/apk/universal/release/`
