# Fork 功能更新清单

本仓库 fork 自 [readest/readest](https://github.com/readest/readest)，本文件记录 fork 之后本仓库新增/修改的功能，便于对照上游了解差异。

- **分叉点**：上游 `main` @ `f6146c217`（`release: version 0.12.6`，2026-08-29）
- **开发时间**：2026-08-29 ～ 2026-09-12
- **提交数**：领先上游 52 个提交（`git log upstream/main..main --oneline` 可复核）
- 相关文档：[FORK-GUIDE.md](./FORK-GUIDE.md)（环境与构建指南）、[ROADMAP.md](./ROADMAP.md)（开发路线）

---

## 1. AI 阅读助手（全新功能）

围绕"阅读时和 AI 角色对话"构建的完整助手体系：

- **多角色助手**：支持创建多个 AI 角色，每个角色绑定头像（内置头像库）与系统提示词 `497b5215e`
- **角色聊天背景**：角色的形象图自动作为聊天界面的背景 `62690b0d4`
- **角色模板**：内置常用角色模板，一键创建助手 `2bc559f88`
- **角色阵容管理**：助手的启用状态与可达性管理 `164b999b2`
- **多供应商连接**：可配置多个 AI 供应商连接（OpenAI 兼容等），按角色绑定；API Key 缺失时按请求提示补录，而不是启动时拦截 `623905635` `4e7bd2741` `3241c8a00`
- **连接为权限单元**：配置模型重构为"连接"为中心，角色作为权限单位 `0d617a904`
- **系统提示词完全可编辑**：全局系统提示词设置 `5ac482fae`；系统提示词跟随连接 `9d3aa6f53`；整套系统提示词变为用户可编辑模板 `7ba80df11`
- **MCP 工具支持**：聊天可接入 MCP 工具服务器，工具按连接绑定、严格按连接显式启用（默认关闭）`42aa5db95` `c0214ecca` `f76b1e7d8`
- **朗读 AI 回复**：一键朗读助手回复 `9198a3eb1`
- **AI 配置导入/导出**：整套 AI 配置（角色、连接、模板）可导出备份、导入恢复 `885466adf`
- **生产构建可用**：AI 助手标签页在生产构建中默认开启 `13b4238c7`；工具栏集成 Ask AI 按钮（含旧数据迁移）`605a04819`
- **稳定性**：修复 assistant-ui detached-call 崩溃；聊天设为默认视图 `d6a3e7271`；书籍索引改为增强而非门禁（不再阻塞使用）`dccd69f14`

## 2. 生词本系统（全新功能）

查词即收藏、复习有语境的生词学习闭环：

- **生词本**：词典查询结果一键存入生词本，保存释义快照 `4221f685c`
- **快照质量**：保留词典原始排版（HTML 快照），长释义可滚动 `6f852a85c` `42aaeb967`
- **并入笔记本**：生词本整合进阅读器侧边栏 Notebook `f52ddba56`
- **跨设备同步**：生词数据通过同步通道在设备间同步 `5cf4cd055`
- **词形归并**（`627e4cd17`）：
  - 英文词形变化模块（屈折/派生 + 词典化词条守卫），划词 running/ran 归并到词条 run
  - 数据库迁移：`surface_forms` 列记录实际遇到过的词形，`vocabulary_meta` 表管理旧数据的一次性重归并
  - 正文生词标记：每个生词出现位置在书页上高亮，点击标记直接打开详情弹窗
  - 详情弹窗：释义（可设主释义）、遇到过的词形、语境卡片（可跳回原文出处）
  - **TTS 发音**：打开弹窗自动读单词；弹窗内单词/按句朗读语境；发音与"朗读文章"完全同款配置（同引擎、同首选声音、同语速）
- **语言设置**：语言面板新增相关配置项，zh-CN/zh-TW 翻译同步更新

## 3. 第三方云同步（个人使用解禁）

- 解除第三方网盘同步的功能门禁，个人可自由使用 `edc38bd19`
- **Google Drive 桌面 Linux 完整打通**（上游仅支持部分平台）：
  - 桌面 Linux loopback OAuth 登录 `086de176e`，及其系列修复：单阶段 loopback 捕获 `454ad0828`、ACL 权限注册 `89f84312f`、失败原因透出 `460ababca`、Desktop 类型 client secret `91198713a`、默认官方 Google 客户端 `9db7e86e7`、Android 端命令注册桌面限定 `2c5403434`
  - 桌面 Linux HTTP 走系统代理 `7736b3090`
- **账户级配置产物同步**：应用配置（含 AI 配置）作为账户级产物走文件同步通道，跨设备漫游 `b4deda132` `69c0c1726`

## 4. 桌面 Linux 适配

- Linux 桌面默认 X11 + 软件渲染，规避 Wayland/EGL 兼容问题 `5b9a35916`

## 5. 阅读器主题

- **背景纹理自动轮换**：阅读背景图可定时自动切换 `2d3b4e448`
- **自选轮换图集**：可挑选哪些图片加入背景轮换 `eaf8b4e0f`
- 图片目录写入 `.nomedia`，避免被相册索引 `e809549f1`

## 6. 工程与构建

- **测试体系复活**：修复全部 546 个失败用例，整套测试（800+ 文件、10000+ 用例）恢复全绿 `a7db55166`
- **统一产物脚本**：`scripts/release-artifacts.sh` 一条命令产出 APK/AppImage 并归集到 `releases/` `758595e2a`
- 开发服务器支持 `NEXT_DEV_ALLOWED_ORIGINS` 放行额外来源 `fe8ab7763` `6d92f62df`
- Android 使用独立包名 `com.bilingify.readest.dev`，可与官方版共存安装

## 7. 文档

- Fork 开发路线图 `b59d839a8`；Fork 交接指南（构建环境、签名、常见坑）`dcb472dac`；Google Drive loopback 接入实录 `e7f596fc5`
